services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: xlb
      POSTGRES_PASSWORD: xlb
      POSTGRES_DB: xlb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U xlb"]
      interval: 5s
      timeout: 5s
      retries: 10

  adminer:
    image: adminer
    ports:
      - "8080:8080"
    depends_on:
      - db

volumes:
  pgdata:
